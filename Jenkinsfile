ipeline {
  agent 
  {
    node {
        label 'windows1' 'windows2'
         }
  }
  stages {
    stage('version') {
      steps {
             powershell 'Write-Output "Hello, World!"'
           }
    }
    
  }
}
